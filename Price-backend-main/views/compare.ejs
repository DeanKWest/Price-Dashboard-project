<main>
  <br> <!-- Deze BR fixt op een of andere manier witruimte bovern de body tag. NIET WEGHALEN! - Sven  -->
  <div class="dashboard">
    <div class="controls">
      <!-- Back-knop -->
      <button type="button" onclick="goBackFromCompare()"
        style="margin-right:1rem; padding:8px 12px; border-radius:4px; border:1px solid #ddd; background:#fff; cursor:pointer;">
        ◀ <%= locals.language.compare.back %>
      </button>

      <select id="timeRange">
        <option value="2"><%= locals.language.compare.dropdown.h2 %></option>
        <option value="24"><%= locals.language.compare.dropdown.h24 %></option>
        <option value="168"><%= locals.language.compare.dropdown.w1 %></option>

        <option value="720" selected><%= locals.language.compare.dropdown.m1 %></option>
        <option value="8760"><%= locals.language.compare.dropdown.y1 %></option>
      </select>

      <button id="tempBtn" class="active"><%= locals.language.compare.temperature %></button>
      <button id="humBtn"><%= locals.language.compare.humidity %></button>
    </div>

    <div class="chart-container-full">
      <canvas id="dataChart"></canvas>
    </div>
  </div>
</main>